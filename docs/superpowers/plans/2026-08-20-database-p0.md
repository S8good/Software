# Database P0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task with verification checkpoints.

**Goal:** Add versioned processing methods, persisted quality-control results, and append-only reanalysis lineage to the isolated iteration copy.

**Architecture:** Extend the existing SQLite migration schema and `DatabaseManager`; keep raw data immutable and store all derived results as new records. Put method canonicalization and QC rules in GUI-independent modules, then add small GUI entry points that call those APIs.

**Tech Stack:** Python 3.9, SQLite, PyQt5, pytest, existing migration runner and structured spectrum tables.

---

### Task 1: Establish migration coverage

**Files:**
- Modify: `nanosense/core/migrations/__init__.py`
- Create: `nanosense/core/migrations/migration_0003_database_p0.py`
- Test: `tests/test_database_p0.py`

- [x] Write tests that create a fresh SQLite connection, run migrations twice, and assert the new columns/tables/indexes exist without duplicate migration records.
- [x] Run `C:\ProgramData\anaconda3\envs\py39\python.exe -m pytest tests/test_database_p0.py -q`; expect RED because the P0 migration and test helpers do not exist.
- [x] Register `migration_0002_snapshot_soft_delete` before the new migration.
- [x] Implement idempotent `0003_database_p0` DDL and indexes.
- [x] Run the migration tests and verify GREEN.

### Task 2: Add canonical processing methods

**Files:**
- Create: `nanosense/core/processing_methods.py`
- Modify: `nanosense/core/database_manager.py`
- Test: `tests/test_database_p0.py`

- [x] Write tests for stable canonical JSON, stable SHA-256 fingerprints, required method fields, method creation, listing, and creation of a new version when parameters change.
- [x] Run the focused tests and confirm the new API fails before implementation.
- [x] Implement canonical payload construction and validation without Qt dependencies.
- [x] Add `create_processing_method`, `list_processing_methods`, and `get_processing_method` to `DatabaseManager`; never update an applied snapshot in place.
- [x] Make structured spectrum persistence include the method fingerprint and preserve the existing raw hash.
- [x] Run focused tests and then the existing database tests.

### Task 3: Add deterministic quality control

**Files:**
- Create: `nanosense/core/quality_control.py`
- Modify: `nanosense/core/database_manager.py`
- Test: `tests/test_database_p0.py`

- [x] Write tests for finite values, monotonic wavelengths, saturation, low signal, and missing reference/background checks, including `pass`, `warning`, and `fail` summaries.
- [x] Run the focused tests and confirm RED.
- [x] Implement pure QC rules returning serializable findings with rule key, severity, value, threshold, and message.
- [x] Add `save_quality_checks` and `get_quality_checks` persistence; update `spectrum_sets.quality_flag` with the worst severity.
- [x] Integrate QC into spectrum save only when an explicit `quality_context` is supplied, preserving existing save behavior for legacy callers.
- [x] Run focused tests and database regression tests.

### Task 4: Add append-only reanalysis lineage

**Files:**
- Modify: `nanosense/core/database_manager.py`
- Modify: `nanosense/core/data_access.py`
- Test: `tests/test_database_p0.py`

- [x] Write a test that saves a raw spectrum, creates two processing snapshots, records two analysis runs, and asserts both runs remain available with distinct processing IDs and source fingerprints.
- [x] Run the focused test and confirm RED.
- [x] Implement `create_analysis_run` with `run_kind`, `processing_config_id`, `parent_analysis_run_id`, and `source_fingerprint`.
- [x] Implement a source-spectrum lookup used by reanalysis and a comparison payload containing metrics and processing metadata.
- [x] Run focused tests and data-access tests.

### Task 5: Add the minimum GUI workflow

**Files:**
- Modify: `nanosense/gui/measurement_widget.py`
- Modify: `nanosense/gui/database_explorer.py`
- Modify: `nanosense/gui/main_window.py`
- Modify: `nanosense/translations/chinese.ts` only if required by existing translation conventions
- Test: `tests/test_database_p0.py` and focused pytest-qt tests

- [x] Write a smoke test for saving/applying a method payload and displaying a QC summary without requiring real hardware.
- [x] Run the smoke test and confirm RED.
- [x] Add method save/apply controls to the processing panel and refresh the method list from `DatabaseManager`.
- [x] Show the latest QC summary in the measurement view and block only `fail` saves when the user explicitly chooses validation.
- [x] Add a database explorer reanalysis action that opens the existing analysis window with the selected raw spectrum and method metadata.
- [x] Run focused Qt tests offscreen.

### Task 6: Documentation, clean database, and release verification

**Files:**
- Modify: `README.md`
- Create: `docs/database_p0_migration.md`
- Create: `tests/test_iteration_database_state.py`

- [x] Add documentation for empty iteration databases, migration order, method immutability, QC severity, and reanalysis lineage.
- [x] Add a test that creates the default iteration database from scratch and confirms no historical experiments or spectra exist.
- [x] Run all py39 tests, compileall, git diff --check, and a Mock API smoke test.
- [x] Rebuild the two Windows artifacts only if packaging files or runtime imports changed. (Portable rebuilt; installer awaits Inno Setup.)
- [x] Commit with a message beginning `feat(database): add P0 method QC and reanalysis lineage`.
- [x] Push a dedicated branch named `database-p0-iteration-20260820` if the configured remote accepts it.
